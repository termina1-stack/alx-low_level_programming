#include <stdio.h>
/**
  * main - program that prints to screen using the function puts
  * Return: 0 (Success)
  */
int main(void)
{
	char s[] = "\"Programming is like building a multilingual puzzle";

	puts(s);

	return (0);
}
